<div class="container">
  <div class="row">
    <h2>農場一覧</h2>
  </div>
  <div class="row mt-3 justify-content-center">
    <div class="col-md-3">
      <%= link_to "新着順", farmer_farms_path(sort: 'created_at DESC'), class: "btn btn-block text-light btn-color"  %>
    </div>
    <div class="col-md-3">
      <%= link_to "人気順", farmer_farm_ranking_path, class: "btn btn-block text-light btn-color"  %>
    </div>

  </div>
    <%= render 'farmer/farms/index', farms: @farms %>
</div>

<%= paginate @farms %>