<h2>マイページ編集</h2>

<%= form_with model: @customer, url: farmer_customer_path, local: true do |f| %>
  <table>
    <tbody>
      <tr>
        <td><%= f.label :名前 %></td>
    　　<td>姓</td>
    　　<td><%= f.text_field :last_name %></td>
    　　<td>名</td>
    　　<td><%= f.text_field :first_name %></td>
      </tr>
      <tr>
        <td><%= f.label :フリガナ %></td>
        <td>セイ</td>
        <td><%= f.text_field :last_name_kana %></td>
        <td>メイ</td>
        <td><%= f.text_field :first_name_kana %></td>
      </tr>
      <tr>
        <td><%= f.label :メールアドレス %></td>
        <td></td>
        <td><%= f.text_field :email %></td>
      </tr>
      <tr>
        <td></td>
        <td></td>
        <td><%= f.submit "更新" %>
            <%= link_to "退会", farmer_path %></td>
      </tr>
    </tbody>
  </table>
<% end %>